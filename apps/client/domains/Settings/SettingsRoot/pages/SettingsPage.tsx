import { FC } from 'react'

import { Page, PageContent, PageHeader } from '@shared/components'

export const SettingsPage: FC = () => {
  return (
    <Page>
      <PageHeader title={'Settings'} />
      <PageContent>{'FORM HERE'}</PageContent>
    </Page>
  )
}
