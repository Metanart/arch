import { TASK_STATUS, TASK_TYPE, TASKS_QUEUE_STATUS } from './enums'

export type TaskType = `${TASK_TYPE}`

export type TaskStatus = `${TASK_STATUS}`

export type TasksQueueStatus = `${TASKS_QUEUE_STATUS}`
